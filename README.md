# OrderBookFetcher

## Installation on Ubuntu with a Script

Run this command in your terminal to automatically install the project:

```bash
curl -sL https://raw.githubusercontent.com/slekrem/OrderBookFetcher/main/install.sh | bash
```
